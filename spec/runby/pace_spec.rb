require 'spec_helper'

describe Runby::Pace do
  it 'has a version number' do
    expect(Runby::Pace::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
